import SkyscraperHero from "@/app/components/industries/something-else/hero/SkyscraperHero";

/**
 * Something Else hero — a skyscraper the camera flies around and up as you
 * scroll, pinned under the site header (see SkyscraperHero).
 */
export default function SomethingElseHero() {
  return <SkyscraperHero />;
}
